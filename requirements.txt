# Qwen2.5-VL LoRA fine-tune. torch는 호스트 드라이버에 맞는 빌드를 먼저 깔 것.
# 🔴 vast에서 pip가 torch를 드라이버보다 최신 CUDA 빌드로 올려버리는 사고가 있었다.
#    설치 후 반드시 torch.cuda.is_available() 과 get_device_name(0) 을 같이 확인.
#    (device_count()만 보면 4로 나와서 정상으로 착각한다)
transformers>=4.49
accelerate>=0.34
peft>=0.13
qwen-vl-utils
pillow
numpy
# YOLO 백엔드 예측을 뽑을 때만 필요
ultralytics>=8.3
